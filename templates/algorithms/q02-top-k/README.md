# Problem: Top-K Frequent Elements

Given a stream of strings (e.g., HTTP request paths), implement a data structure that can return the top K most frequent elements at any point. The stream is too large to store entirely.
