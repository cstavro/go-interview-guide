# Problem: Sliding Window Rate Limiter

Given a stream of requests (each with a user ID and timestamp), implement a function that checks if a request is allowed based on a sliding window limit: no more than N requests per user in the last M seconds.
