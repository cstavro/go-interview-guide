# Problem: Diff of Sorted Slices

Given two sorted slices of strings, compute the difference: elements only in A, elements only in B, and elements in both. Optimize for minimal allocations and single-pass where possible.
