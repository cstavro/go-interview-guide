# Problem: Token Bucket Rate Limiter

Implement a TokenBucket that allows bursts up to a maximum capacity but refills at a fixed rate. Provide Allow() and AllowN(n int) methods. Must be safe for concurrent use.
