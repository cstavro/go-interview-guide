# Problem: Semaphore Using Channels

A semaphore limits the number of concurrent operations. Implement one using only channels (no sync package). Provide Acquire and Release methods. Include a test that proves it limits concurrency to N.
