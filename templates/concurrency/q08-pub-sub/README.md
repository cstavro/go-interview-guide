# Problem: Pub/Sub with Topic Filtering

Build a pub/sub system where subscribers can subscribe to topics with wildcards (e.g., `orders.*`). Support concurrent publishers and subscribers. Subscribers receive messages via a channel. Ensure slow subscribers don't block fast publishers.
