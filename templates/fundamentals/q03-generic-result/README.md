# Problem: Generic Result Type

Go does not have a built-in `Result<T, E>` type like Rust. Design and implement a generic `Result[T]` type that holds either a value or an error. Provide `Map`, `MapErr`, and `OrElse` methods. Demonstrate usage with a chain of operations that might fail.
