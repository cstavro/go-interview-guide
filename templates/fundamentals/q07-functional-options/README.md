# Problem: Functional Options

Implement a `NewServer` constructor with options for `WithPort`, `WithTimeout`, `WithLogger`, and `WithTLS`. The server should have sensible defaults. Include validation that returns an error if an invalid combination is provided.
