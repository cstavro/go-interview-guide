# Problem: Interface Satisfaction

Go interfaces are satisfied implicitly. Write a small package that defines a `Notifier` interface and two implementations (Email, SMS). Ensure that if either implementation fails to satisfy the interface, the code fails to compile.
